# Spext

Use the Spext MCP tools when the user wants to:

- Share text with `create_text_share`.
- Create a private note with `create_secret_note`.
- Create a link for others to upload files with `create_file_request`.

Confirm ambiguous content or options before creating a link. Return the resulting Spext URL clearly.